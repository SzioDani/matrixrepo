# Stub per requests_toolbelt.utils.dump - usato solo per debug in cloudscraper
def dump_response(response, request_prefix=b'< ', response_prefix=b'> '):
    return b''
def dump_all(response, request_prefix=b'< ', response_prefix=b'> '):
    return b''
